//
// Prefix header for all source files of the 'callback-mac' target in the 'callback-mac' project
//

#ifdef __OBJC__

	#ifdef _DEBUG
	#define DebugNSLog(format, ...) NSLog(format, ## __VA_ARGS__)
	#else
	#define DebugNSLog(format, ...)
	#endif

    #import <Cocoa/Cocoa.h>

	#import "Constants.h"
	#import "Utils.h"
#endif
